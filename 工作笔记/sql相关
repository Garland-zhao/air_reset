1 设置默认字符集为utf8mb4
    https://blog.fearcat.in/a?ID=01700-9b6e47eb-7526-47b1-a761-f4845c0ae504

2 基础数据类型(需要记住)
    https://www.w3school.com.cn/sql/sql_datatypes.asp

3 自增字段 AUTO_INCREMENT
    https://www.w3school.com.cn/sql/sql_autoincrement.asp

4 MySQL查询分析器EXPLAIN或DESC用法
    https://blog.csdn.net/helloxiaozhe/article/details/79951354

5 给字段添加说明--COMMENT
    CREATE TABLE test.game(
        nId INT PRIMARY KEY AUTO_INCREMENT COMMENT '设置主键自增',
        szName VARCHAR(128) COMMENT '游戏名字',
        szPath VARCHAR(256) COMMENT '下载路径'
        ) COMMENT='表注释';

6 查看表信息, 返回创建表的sql
     show create table table_name;


6 mysql 给表添加唯一约束、联合唯一约束，指定唯一约束的名字
    1 单列唯一约束unique:
        建表时:
            CREATE TABLE `t_user` (
                `Id` int(11) NOT NULL AUTO_INCREMENT,
                `username` varchar(18) NOT NULL unique, -- 给username添加唯一约束
                `password` varchar(18) NOT NULL,
                PRIMARY KEY (`Id`)
            ) ENGINE=InnoDB AUTO_INCREMENT=1018 DEFAULT CHARSET=gbk;
        已有表:
            ALTER TABLE `表名称` ADD unique(`字段名称`);
            或者
            ALTER TABLE table_name ADD UNIQUE KEY `约束名` (字段名);
            或者
            create unique index UserNameIndex on '表名称' ('字段名称');

    2 多列联合唯一约束
        建表时(UNIQUE KEY):
            CREATE TABLE `jw_resource` (
              `id` BIGINT(20) NOT NULL AUTO_INCREMENT,
              `resource_name` VARCHAR(128) DEFAULT NULL,
              `resource_type` TINYINT(4) DEFAULT NULL,
              PRIMARY KEY (`id`),
              UNIQUE KEY `resource_name` (`resource_name`,`resource_type`) -- 唯一键约束添加后，在建表的元数据中，默认的唯一键约束名称为第一列的名称。
            ) ENGINE=INNODB AUTO_INCREMENT=2 DEFAULT CHARSET=gbk;
        已有表:
            ALTER TABLE jw_resource ADD UNIQUE KEY(resource_name, resource_type);

    3 删除唯一约束
        ALTER TABLE jw_resource DROP INDEX `resource_name`; -- 唯一键约束添加后，实际上建立了一个索引，将该索引删除后，就等于删除了联合唯一约束。

    4 查看约束
        show index from jw_resource;
